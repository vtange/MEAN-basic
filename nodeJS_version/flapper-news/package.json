{
  "name": "flapper-news",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www"
  },
  "dependencies": {
    "express": "~4.13.1",       //main structure, includes "path"
    "serve-favicon": "~2.3.0",  //enable custom favicon
    "morgan": "~1.6.1",         //express console.logger, can replace with "morgan-debug"
    "cookie-parser": "~1.3.5",  //parses cookies. replaced with "express-session" Mar 2015
    "body-parser": "~1.13.2",   //parses JSON bodies. obsolete and vulnerable to infinite temp file attack
    "debug": "~2.2.0",          //custom console.logger, can replace with "morgan-debug"
    "ejs": "~2.3.3",            //use EJS files for direct HTML conversion
    "mongoose": "^4.2.2"        //mongoDB
  }
}
